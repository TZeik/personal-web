export interface Experience {
  workplace: string;
  position: string;
  startDate: string;
  endDate: string;
  information: string[];
}

export interface Skill {
    name: string;
    imageUrl: string;
}

export interface Work {
    
}